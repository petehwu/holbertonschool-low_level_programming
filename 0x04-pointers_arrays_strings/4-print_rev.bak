#include "holberton.h"
/**
 * print_rev - prints a string in reverse
 * @s: string to reverse print
 */
void print_rev(char *s)
{
	int x = 0;

	while (*s != '\0')
	{
		x++;
		s++;
	}
	for ( ; x >= 0; x--)
		_putchar(*(s--));
	_putchar('\n');
}
